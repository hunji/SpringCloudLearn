spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hunji/SpringCloudLearn
          search-paths: config
      label: master
server:
  port: 8888
